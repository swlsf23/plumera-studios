Self-hosted Inter (SIL Open Font License).

Source: https://github.com/rsms/inter v4.1
File: InterVariable.woff2 (full variable font; weights via font-weight).
License: Inter-LICENSE.txt

Live pages register this file as CSS family **Plumera Sans** (not `Inter`) so
third-party `@font-face { font-family: Inter }` injections cannot hijack the
stack. `@font-face` `src` uses a query fingerprint (e.g. `?v=inter-2`); bump
it when replacing this file. Deploy Cache-Control: see `docs/deploy.md`.
